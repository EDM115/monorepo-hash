name: Benchmarks the CLI tool

on:
  push:
    branches:
      - master
    tags:
      - 'v*.*.*'
  pull_request:
    types: [labeled]
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      ref:
        description: 'Git reference (branch, tag, or SHA)'
        required: false
        default: 'master'

jobs:
  benchmark:
    if: ${{ github.event_name != 'pull_request' || (github.event_name == 'pull_request' && github.event.label.name == 'benchmark-this') }}
    name: "Benchmark CLI tool"
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Get the Git reference
        id: get_ref
        run: |
          if [[ "${{ github.event.inputs.ref }}" != "" ]]; then
            echo "ref=${{ github.event.inputs.ref }}" >> $GITHUB_OUTPUT
          else
            ref="${{ github.ref }}"
            echo "ref=${ref##*/}" >> $GITHUB_OUTPUT
          fi

      - name: "Checkout code at ${{ steps.get_ref.outputs.ref }}"
        uses: actions/checkout@v6
        with:
          ref: ${{ steps.get_ref.outputs.ref }}

      - name: "Install pnpm"
        uses: pnpm/action-setup@v4

      - name: "Bump Node.js to v24"
        uses: actions/setup-node@v6
        with:
          check-latest: true
          cache: "pnpm"
          node-version: lts/krypton

      - name: "Install Bun"
        uses: oven-sh/setup-bun@v2

      - name: "Install dependencies"
        run: |
          pnpm i --frozen-lockfile
          wget https://github.com/sharkdp/hyperfine/releases/download/v1.20.0/hyperfine_1.20.0_amd64.deb
          sudo dpkg -i hyperfine_1.20.0_amd64.deb
          rm hyperfine_1.20.0_amd64.deb
          sudo add-apt-repository ppa:zhangsongcui3371/fastfetch -y
          sudo apt-get update
          sudo apt-get install fastfetch lshw -y

      - name: "Get the system specs"
        run: |
          echo "System Specs :"
          echo "CPU : $(lscpu | grep 'Model name')"
          echo "RAM : $(free -h | grep 'Mem' | awk '{print $2}')"
          echo "Disk : $(df -h | grep '/$' | awk '{print $2}')"
          echo "OS : $(lsb_release -d | awk -F'\t' '{print $2}')"
          echo "Fastfetch :"
          fastfetch -c all.jsonc
          echo "lshw :"
          sudo lshw -short

      - name: "Prepare the benchmarks"
        run: |
          pnpm build && pnpm build:bun:linux-x64
          chmod +x bun-build/monorepo-hash-linux-x64
          mkdir -p tests/benchmarks/node tests/benchmarks/bun
          7z x tests/demo/small-monorepo.7z -o./tests/demo
          cp dist/monorepo-hash.mjs tests/demo/small-monorepo/monorepo-hash.mjs
          cp bun-build/monorepo-hash-linux-x64 tests/demo/small-monorepo/monorepo-hash
          7z x tests/demo/medium-monorepo.7z -o./tests/demo
          cp dist/monorepo-hash.mjs tests/demo/medium-monorepo/monorepo-hash.mjs
          cp bun-build/monorepo-hash-linux-x64 tests/demo/medium-monorepo/monorepo-hash
          7z x tests/demo/large-monorepo.7z -o./tests/demo
          cp dist/monorepo-hash.mjs tests/demo/large-monorepo/monorepo-hash.mjs
          cp bun-build/monorepo-hash-linux-x64 tests/demo/large-monorepo/monorepo-hash

      - name: "Benchmark : small monorepo, bun"
        run: |
          cd tests/demo/small-monorepo
          hyperfine \
            --prepare 'sync; echo 3 | sudo tee /proc/sys/vm/drop_caches' \
            --warmup 2 \
            --runs 10 \
            --export-markdown ../../benchmarks/bun/small-monorepo.md \
            './monorepo-hash --generate -pm=pnpm'

      - name: "Benchmark : medium monorepo, bun"
        run: |
          cd tests/demo/medium-monorepo
          hyperfine \
            --prepare 'sync; echo 3 | sudo tee /proc/sys/vm/drop_caches' \
            --warmup 2 \
            --runs 10 \
            --export-markdown ../../benchmarks/bun/medium-monorepo.md \
            './monorepo-hash --generate -pm=pnpm'

      - name: "Benchmark : large monorepo, bun"
        run: |
          cd tests/demo/large-monorepo
          hyperfine \
            --prepare 'sync; echo 3 | sudo tee /proc/sys/vm/drop_caches' \
            --warmup 2 \
            --runs 10 \
            --export-markdown ../../benchmarks/bun/large-monorepo.md \
            './monorepo-hash --generate -pm=pnpm'

      - name: "Benchmark : small monorepo, node"
        run: |
          cd tests/demo/small-monorepo
          hyperfine \
            --prepare 'sync; echo 3 | sudo tee /proc/sys/vm/drop_caches' \
            --warmup 2 \
            --runs 10 \
            --export-markdown ../../benchmarks/node/small-monorepo.md \
            'node monorepo-hash.mjs --generate -pm=pnpm'

      - name: "Benchmark : medium monorepo, node"
        run: |
          cd tests/demo/medium-monorepo
          hyperfine \
            --prepare 'sync; echo 3 | sudo tee /proc/sys/vm/drop_caches' \
            --warmup 2 \
            --runs 10 \
            --export-markdown ../../benchmarks/node/medium-monorepo.md \
            'node monorepo-hash.mjs --generate -pm=pnpm'

      - name: "Benchmark : large monorepo, node"
        run: |
          cd tests/demo/large-monorepo
          hyperfine \
            --prepare 'sync; echo 3 | sudo tee /proc/sys/vm/drop_caches' \
            --warmup 2 \
            --runs 10 \
            --export-markdown ../../benchmarks/node/large-monorepo.md \
            'node monorepo-hash.mjs --generate -pm=pnpm'

      - name: "Upload benchmarks results"
        uses: actions/upload-artifact@v5
        with:
          name: benchmarks-results-${{ steps.get_ref.outputs.ref }}
          path: tests/benchmarks/**/*.md
